name: "Standard Tier: Automated Snowflake Delivery - Master"
on:
  workflow_dispatch:
  pull_request:
    types: [opened, synchronize]
    branches:
      - qa
      - cicd
      - main
    paths:
      - 'dcm_automation/**'
      - 'dcm_dbt_cicd/**'
  push:
    branches:
      - qa
      - cicd
      - main
    paths:
      - 'dcm_automation/**'
      - 'dcm_dbt_cicd/**'

permissions:
  id-token: write 
  contents: read

jobs:
  qa-environment-pipeline:
    if: (github.base_ref == 'qa' && github.event_name == 'pull_request') || (github.ref_name == 'qa' && github.event_name == 'push')
    uses: ./.github/workflows/snowflake-pipeline-qa.yml
    secrets: inherit

  cicd-environment-pipeline:
    if: (github.base_ref == 'cicd' && github.event_name == 'pull_request') || (github.ref_name == 'cicd' && github.event_name == 'push')
    uses: ./.github/workflows/snowflake-pipeline-cicd.yml
    secrets: inherit

  main-environment-pipeline:
    if: (github.base_ref == 'main' && github.event_name == 'pull_request') || (github.ref_name == 'main' && github.event_name == 'push')
    uses: ./.github/workflows/snowflake-pipeline-main.yml
    secrets: inherit
